# Rejected references

Add rejected figures here with a same-stem Markdown review. Classify the rejection as color, hierarchy, chart type, layout, typography, annotation, scientific expression, or other. Keep the record so the same failure is not repeated.

