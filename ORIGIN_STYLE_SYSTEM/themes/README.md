# Origin themes

Theme creation is not exposed as a dedicated tool in the audited MCP version. Round one therefore treats verified graph templates as the reusable source of truth.

If a documented Origin Theme save/apply route is later tested through LabTalk, store the verified artifact here and record which properties it controls. Never create a fake Theme file for directory completeness.

